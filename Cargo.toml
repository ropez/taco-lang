[package]
name = "taco"
version = "0.1.0"
edition = "2024"

[features]
default = ["fs", "process", "json"]
fs = []
process = []
json = ["dep:tinyjson"]

[dependencies]
multipeek = "0.1.2"
smol = { version = "2.0.2" }
tinyjson = { version = "2.5.1", optional = true }

